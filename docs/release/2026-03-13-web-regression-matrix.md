# Web 首发回归矩阵（2026-03-13）

## A. 账号体系
### 已验证
- 注册接口可用
- 登录接口可用
- `me` 可返回当前用户
- 退出登录前端链路已接通

### 待手测
- 登录页输入错误密码提示
- 注册重复用户名提示
- 退出登录后返回登录页
- 刷新页面后自动恢复登录态

## B. 首页 / 学习链路
### 已验证
- 首页 UI 主链路已切到手机端样式
- 开始学习默认 30 词
- `words/session` 可正常返回题目
- `progress/sync` 成功写入 XP / Coin / 成就
- 结果页已接入新版手机端 UI

### 待手测
- 首页按钮跳转是否都正常
- 开始学习后 30 题是否稳定
- 答题后 XP/金币/成就弹窗视觉是否正常
- 学习完成后返回首页数据是否刷新

## C. 复习 / 词表
### 已验证
- `progress/reviews` 可正常返回
- 词表页 UI 已对齐手机端风格

### 待手测
- 待复习入口在有数据时可进入
- 无复习数据时提示是否自然
- 词表搜索是否符合预期
- 词表朗读按钮是否正常

## D. 排行 / 社交 / 成就
### 已验证
- `leaderboard` 接口可用
- `social/search` 接口可用
- 排行页 / 成就墙 UI 已对齐手机端风格
- 隐藏成就未解锁时不展示

### 待手测
- 排行页显示是否正常
- 动态页空状态是否自然
- 成就墙分类是否完整
- 成就解锁弹窗是否和页面风格一致

## E. PK
### 已完成
- PK 页面手机端 UI 已重构
- WebSocket 前端已迁到 token 模型
- 后端 `pk` 网关已存在

### 待手测
- 人机模式进入是否稳定
- 对战中分数推进是否正常
- 退出 PK 是否能正确回首页
- 真人匹配链路是否正常

## F. 设置
### 已完成
- 设置页手机端 UI 已重构
- 重置进度接口已接通
- OpenID 展示行已保留

### 待手测
- 音效开关交互
- 语言切换交互
- 重置进度后首页是否清空
- 退出登录是否稳定

## G. 部署
### 已完成
- PM2 配置文件已提供
- `.env.example` 已提供
- 部署命令版文档已提供

### 待执行
- 上服务器安装依赖
- 构建前后端
- 配置 `.env`
- 配置 Nginx
- 首次生产回归

## 当前建议的首发阻断项
1. 登录/注册/恢复/退出 必须全手测通过
2. 开始学习 30 词 + `progress/sync` 必须稳定
3. 设置页重置进度必须手测通过
4. PK 至少人机模式必须稳定

## 可暂缓项
1. 真人 PK 的复杂边缘情况
2. 社交关注/取关深度验证
3. 更复杂的桌面端适配
